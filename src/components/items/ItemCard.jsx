import React from 'react'
import { observer, inject } from 'mobx-react'

const ItemCard = inject('')(observer(props => {
    return (
        <div>
            
        </div>
    )
}))

export default ItemCard
